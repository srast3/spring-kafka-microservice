spring:
  profile:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/srast3/spring-kafka-ms.git/
          search-paths:
            - "EmailServiceConfigProperties/{application}/{profile}"

